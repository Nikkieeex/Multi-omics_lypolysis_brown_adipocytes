---
title: "Logboek_Gea"
output: html_document
---

Auteur: Gea Bakker
Project: *Omics, Multi-omics analysis of thermogenic lypolysis in brown adipocytes
Repository: https://github.com/Nikkieeex/Multi-omics_lypolysis_brown_adipocytes.git 
Wetenschappelijk artikel: https://www.sciencedirect.com/science/article/pii/S2589004225016438 
Groepsgenoten: Janine Postmus, Joris Deelstra, Nicole Bovenga
Deadline: 2026-05-04

```{r libraries, include=FALSE}
library(Rsamtools)
library(dplyr)
library(tidyr)
library(ggplot2)
library(pheatmap)
library(ggrepel)
library(PCAtools)
library(readxl)
library(stringr)
```

### Gebruikte tools met versies
```{r tool-versies, echo=FALSE}
packages <- c(
  "Rsamtools",
  "dplyr",
  "tidyr",
  "ggplot2",
  "pheatmap",
  "ggrepel",
  "PCAtools",
  "readxl",
  "stringr"
)

tool_versions <- data.frame(
  Tool = c("R", packages),
  Versie = c(
    R.version.string,
    sapply(packages, function(pkg) as.character(packageVersion(pkg)))
  )
)

knitr::kable(tool_versions, row.names = FALSE)
```


### Logboek van Gea Bakker
Dit logboek doorloopt de handelingen die uitgevoerd zijn door Gea Bakker en toont de bijdrage voor het project. Het uiteindelijke product van dit project is een wetenschappelijk artikel waarbij de data van het bestaande wetenschappelijk artikel gebruikt is. Voor ons artikel wordt er een nieuwe onderzoeksvraag bedacht die te beantwoorden is met deze data en mogelijk extra literatuur onderzoek. 

#### Samenvatting wetenschappelijk artikel

### Data inlezen en exploratieve verkenning
Voor de data verder verwerkt wordt, voer ik eerst een exploratieve data-analyse uit op de BAM-files. Hierbij lees ik de bestanden in en controleer ik de aantal reads per bestand en het aantal gemapte en ongemapte reads. 

Bij een globale verkenning van de map met BAM-files viel op dat er geen bijbehorende BAI-files aanwezig waren. BAI-files zijn indexbestanden voor BAM-files en zijn nodig voor bepaalde analyses, zoals het snel opvragen van informatie per chromosoom of contig. Daarom worden de BAM-files eerst geïndexeerd voordat de verdere verkenning wordt uitgevoerd. 

Nadat dit allemaal uitgevoerd is ga ik met featureCounts een count tabel maken. Hierbij worden alignments uit de BAM-files gekoppeld aan genen of features uit de annotatie. De count tabel geeft per gen weer hoeveel reads aan elk sample zijn toegewezen.

```{r BAM files inlezen, include=FALSE}
bam_files <- list.files(
  path = "/students/2025-2026/Thema08/lipolyse/unpacked/transcriptomics",
  pattern = "\\.bam$",
  full.names = TRUE
)

```

```{r BAI-files maken, eval=FALSE}
indexBam(bam_files)

```

Voor de zekerheid check ik even of er voor alle BAM-files nu een BAI-file gemaakt is. Dit om te voorkomen dat er verderop in de verkenning errors ontstaan. 

```{r BAI-controle}
data.frame(
  bam = basename(bam_files),
  index_exists = file.exists(paste0(bam_files, ".bai"))
)
```

Nu er voor elke BAM-file een BAI-file gemaakt is, ga ik verder met de verkenning. Ik ga eerst kijken naar de total reads, mapped reads, unmapped reads en de percentage gemapte reads. 

```{r BAM-verkenning}
bam_summary <- data.frame(
  sample = basename(bam_files),
  total_records = NA,
  mapped = NA,
  unmapped = NA,
  mapping_percentage = NA
)

for (i in seq_along(bam_files)) {
  stats <- idxstatsBam(bam_files[i])
  
  bam_summary$total_records[i] <- sum(stats$mapped + stats$unmapped)
  bam_summary$mapped[i] <- sum(stats$mapped)
  bam_summary$unmapped[i] <- sum(stats$unmapped)
  bam_summary$mapping_percentage[i] <- round(
    bam_summary$mapped[i] / bam_summary$total_records[i] * 100,
    2
  )
}

cat("Laagste aantal mapped reads:", min(bam_summary$mapped), "\n")
cat("Hoogste aantal mapped reads:", max(bam_summary$mapped), "\n")
cat("Gemiddeld mapping percentage:", mean(bam_summary$mapping_percentage), "\n")
cat("Aantal samples met unmapped reads:", sum(bam_summary$unmapped > 0), "\n")
```

Het is opvallend dat voor elk BAM-file een mapping percentage van 100% werd gevonden. Dit komt waarschijnlijk doordat alleen de gemapte alignments in de BAM-files opgenomen zijn en de ongemapte reads kunnen mogelijk weggeschreven zijn in andere outputbestanden of vermeld worden in logbestanden. 

Het laagste aantal gemapte alignments is 40.794.932. Dit laat zien dat ook het kleinste bestand nog een groot aantal alignments bevat en dat de bestanden voldoende data bevatten voor verdere verwerking.

Na deze controle ga ik een count tabel maken met featureCounts. Hierbij worden de alignments uit de BAM-files gekoppeld aan genen of features uit de annotatie. De count tabel geeft per gen weer hoeveel reads aan elk sample zijn toegewezen. Deze tabel kan ik vervolgens gebruiken als basis voor de verdere exploratieve data-analyse.

De count tabel heb ik gemaakt door de onderstaande code uit te voeren in de terminal.
```{bash, eval=FALSE}
featureCounts -T 64 -p -a ref_genome/Mus_musculus.GRCm39.112.gtf -o counts.txt *.bam
```

Door "*.bam" te gebruiken worden alle BAM-files tot 1 count tabel verwerkt. Met de optie -a is het annotatiebestand van het referentie genoom van de muis meegegeven. De optie -o geeft aan dat de output wordt opgeslagen als counts.txt in dezelfde map waar de BAM-files in staan. Met -T 64 worden er 64 threads gebruikt om de analyse sneller uit te voeren. De optie -p geeft aan dat de data paired-end reads bevat. 

Counts.txt bevat nu een count tabel waarin per gen staat hoeveel reads aan elk sample zijn toegewezen. Deze count tabel wordt vervolgens ingelezen en gebruikt voor verdere exploratieve data-analyse.

Voor de echte verkenning ga ik eerst controleren of de count tabel goed ingelezen wordt. Dit doe ik door te controleren wat de head is, de aantal kolommen en rijen en de titels van de kolommen.

```{r}
counts <- read.delim(
  "/students/2025-2026/Thema08/lipolyse/unpacked/transcriptomics/counts.txt",
  comment.char = "#",
  check.names = FALSE
)

str(counts)
```

De count tabel is ingelezen met read.delim(). Vervolgens is er met head() en str() gecontroleerd of de tabel correct is ingelezen. Ik krijg 57.186 features en 24 kolommen terug. De eerste kolommen bevatten geninformatie, zoals GeneID, Chr, start, end, strand en length. De rest van de kolommen bevatten de read counts per sample. Ik heb ook even gekeken naar de datatypes van de kolommen en deze zien er bruikbaar uit. De kolommen die de counts bevatten zijn integers en de kolom met de gen id is een character, dit is bruikbaar voor verdere verwerking. 

Voor een snelle verwerking ga ik eerst een data frame maken met alleen de gen id en de counts per sample. 
```{r}
gene_info <- counts[, 1:6]
count_matrix <- counts[, 7:ncol(counts)]

rownames(count_matrix) <- counts$Geneid

count_matrix <- as.data.frame(lapply(count_matrix, as.numeric))
rownames(count_matrix) <- counts$Geneid

```

Ik ga eerst kijken naar de verdeling van de counts per sample met behulp van een boxplot. Hiermee kan ik snel controleren of de samples een vergelijkbare countverdeling hebben en of er sprake is van extreme waarden of scheefheid in de data. RNA-seq countdata bevat vaak veel lage waarden en enkele zeer hoge waarden. Hierdoor verwacht ik dat de counts niet normaal verdeeld zijn.

```{r, echo=FALSE}
raw_long <- count_matrix %>%
  mutate(Geneid = rownames(count_matrix)) %>%
  pivot_longer(
    cols = -Geneid,
    names_to = "sample",
    values_to = "count"
  )


ggplot(raw_long, aes(x = sample, y = count)) + 
  geom_boxplot(outlier.size = 0.5) +
  theme_bw() +
  theme(axis.text.x = element_text(angle = 90, hjust = 1)) +
  labs(
    title = "Verdeling van ruwe counts per sample",
    x = "Sample",
    y = "Ruwe count"
  )
```

Deze boxplot laat zien dat de meeste waarden laag zijn, terwijl enkele genen extreem hoge counts hebben. Hierdoor wordt de y-as sterk beinvloed door outliers, waardoor de boxplots visueel plat gedrukt zijn. Door deze scheve verdeling zijn de samples moeilijk te vergelijken.

Deze scheve verdeling zorgt ervoor dat ik als vervolg stap een logtransformatie ga uitvoeren. De logtransformatie maakt de extreme waarden minder dominant en dat maakt de verdeling van de counts beter zichtbaar. 

Omdat ik veel '0' heb in mijn counts, voeg ik een +1 toe in mijn log. Dit komt er dan uit te zien als log2(count + 1). Een log uitvoeren op 0 levert '-Inf' op, wat min oneindig betekent en hier kan ik niet mee verder rekenen. Log2(0 + 1) levert 0 op, dus de 0 counts worden niet beinvloed door de logtransformatie.

```{r, echo=FALSE}
log_counts <- log2(count_matrix + 1)

log_long <- log_counts %>%
  mutate(Geneid = rownames(log_counts)) %>%
  pivot_longer(
    cols = -Geneid,
    names_to = "sample",
    values_to = "log_count"
  )

ggplot(log_long, aes(x = sample, y = log_count)) +
  geom_boxplot(outlier.size = 0.5) +
  theme_bw() +
  theme(axis.text.x = element_text(angle = 90, hjust = 1)) +
  labs(
    title = "Verdeling counts per sample na logtransformatie",
    x = "Sample",
    y = "log2(count + 1)"
  )
```

Na de logtransformatie is de verdeling van de counts per sample duidelijker zichtbaar dan zonder deze transformatie. De extreme waarden hebben minder invloed op de schaal van de y-as waardoor de boxplots beter met elkaar vergeleken kunnen worden. De meeste samples laten een vergelijkbare verdeling zien, maar er blijven outliers aanwezig. Dit is normaal bij RNA-seq countdata, omdat sommige genen veel hoger tot expressie komen dan andere genen.

De logtransformatie vermindert de scheefheid, maar corrigeert nog niet voor de verschillen in library size tussen samples. Library size geeft het totaal aantal getelde reads/fragmenten aan per sample. Dit ga ik dus doen omdat verschillen in sequencingdiepte invloed kunnen hebben op de countwaarden tussen samples.

```{r, echo=FALSE}
library_sizes <- colSums(count_matrix)

library_df <- data.frame(
  sample = names(library_sizes),
  library_size = library_sizes
)

ggplot(library_df, aes(x = sample, y = library_size)) + 
  geom_col() + 
  theme_bw() +
  theme(axis.text.x = element_text(angle = 90, hjust = 1)) +
  labs(
    title = "library size per sample",
    x = "Sample",
    y = "Totaal aantal toegewezen reads/fragments"
  )
```

De library size is bepaald om per sample alle reads bij elkaar op te tellen. In de staafdiagram is te zien dat er grote verschillen zijn in library size tussen de samples, een aantal samples hebben 2.0e+07 reads en sommigen hebben 5.0e+07 tot 6.0e+07 reads. Dit toont aan dat het niet eerlijk is om de sample counts direct met elkaar te vergelijken. Om deze reden ga ik een normalisatie uitvoeren om te corrigeren voor deze grote verschillen. 



```{r, echo=FALSE}
cpm_counts <- sweep(
  count_matrix,
  2,
  colSums(count_matrix),
  FUN = "/"
) * 1e6

log_cpm <- log2(cpm_counts + 1)

log_cpm_long <- as.data.frame(log_cpm) %>%
  mutate(Geneid = rownames(log_cpm)) %>%
  pivot_longer(
    cols = -Geneid,
    names_to = "sample",
    values_to = "log_cpm"
  )

ggplot(log_cpm_long, aes(x = sample, y = log_cpm)) + 
  geom_boxplot(outlier.size = 0.5) +
  theme_bw() + 
  theme(axis.text.x = element_text(angle = 90, hjust = 1)) +
  labs(
    title = "Verdeling na sample-normalisatie",
    x = "Sample",
    y = "log2(CPM + 1)"
  )
```

Na de sample-normalisatie, waarbij de counts per sample gedeeld is door de totale library size van dat sample, waarna het vermenigvuldigd is met 1 miljoen. Vervolgens is er nog een log transformatie uitgevoerd zodat de verdeling beter zichtbaar is.

In de boxplot na de sample-normalisatie zijn de samples beter met elkaar te vergelijken dan de ruwe counts. De verschillen in library size zijn nu gecorrigeerd, maar er zijn nog steeds outliers zichtbaar. Dit is niet te voorkomen, omdat sommige genen dusdanig meer tot expressie komen dan andere. Maar na deze normalisatie zijn de samples een stuk beter te vergelijken.

Als laatste voer ik een feature-normalisatie uit met de z-score. Hierbij wordt per gen de gemiddelde expressie afgetrokken en gedeeld door de standaarddeviatie. Hierdoor worden de waarden per gen geschaald, zodat zichtbaar wordt welke samples relatief hoge of lage waarden hebben voor een bepaald gen. Deze stap is vooral nuttig om expressiepatronen tussen samples te vergelijken.


```{r, echo=FALSE}
z_counts <- t(scale(t(log_cpm)))

z_counts <- z_counts[complete.cases(z_counts),]

z_long <- as.data.frame(z_counts) %>%
  mutate(Geneid = rownames(z_counts)) %>%
  pivot_longer(
    cols = -Geneid,
    names_to = "sample",
    values_to = "z_score"
  )

ggplot(z_long, aes(x = sample, y = z_score)) +
  geom_boxplot(outlier.size = 0.5) +
  theme_bw() +
  theme(axis.text.x = element_text(angle = 90, hjust = 1)) +
  labs(
    title = "Verdeling na feature-normalisatie: z-score per gen",
    x = "Sample",
    y = "Z-score"
  )
```

In de boxplot na de feature-normalisatie liggen de meeste samples redelijk rond een z-score van 0. De sample SRR32635416 valt duidelijk op omdat de verdeling lager ligt dan bij de andere samples. Dit betekent dat dit sample voor veel genen relatief lagere genormaliseerde waarden heeft dan de andere samples. Deze uitkomst maakt het interessant om dit sample wat uitgebreider te onderzoeken wat precies de reden is van deze afwijking. 

Omdat de sample SRR32635416 zo opvallend is ga ik daar specifiek naar kijken. Ik ga een PCA uitvoeren op de getransformeerde CPM-data, waarbij dit sample specifiek gemarkeerd wordt.


```{r, echo=FALSE}
pca_logcpm_result <- prcomp(
  t(log_cpm),
  center = TRUE,
  scale. = FALSE
)

pca_logcpm_variance <- pca_logcpm_result$sdev^2
pca_logcpm_variance_percent <- round(
  pca_logcpm_variance / sum(pca_logcpm_variance) * 100,
  1
)

pca_logcpm_df <- data.frame(
  sample = rownames(pca_logcpm_result$x),
  PC1 = pca_logcpm_result$x[, 1],
  PC2 = pca_logcpm_result$x[, 2]
)

pca_logcpm_df$sample_group <- ifelse(
  grepl("SRR32635416", pca_logcpm_df$sample),
  "SRR32635416",
  "Overige samples"
)

ggplot(pca_logcpm_df, aes(x = PC1, y = PC2, color = sample_group)) +
  geom_point(size = 3) +
  geom_text(
    aes(label = ifelse(sample_group == "SRR32635416", sample, "")),
    vjust = -0.8,
    size = 3,
    show.legend = FALSE
  ) +
  theme_bw() +
  labs(
    title = "PCA van log2(CPM + 1) data",
    x = paste0("PC1 (", pca_logcpm_variance_percent[1], "%)"),
    y = paste0("PC2 (", pca_logcpm_variance_percent[2], "%)"),
    color = "Sample"
  )
```
In de PCA is wel te zien dat SRR3265416 zich onderscheidt van de andere samples op PC2. PC2 verklaart 3.6% van de variatie. Het ondersteunt wel de eerdere observatie dat deze sample op een bepaalde manier afwijkt van de rest. In de PCA is ook een ander sample die sterker afwijkt op PC1, die met 67.3% voor de meeste variatie zorgt. Deze sample is ook interessant om verder te bekijken. 

Ik ga eerst kijken welke samples precies afwijken in de PCA. Hier krijg ik de namen van de samples.

#### PCA verbetering 
Deze code is tussendoor toegevoegd omdat ik deze output nodig heb voor code die ik eerder heb geschreven.

Afgelopen weekend ben ik erachter gekomen dat mijn pca elke keer verandert wanneer ik deze uitvoer. Janine heeft mij daarmee geholpen om een tool te vinden die PCA kan maken die constant blijft. Dit is gelukkig gelukt met de library PCAtools. De code heb ik uit haar logboek overgenomen omdat deze uitkomst nodig is voor een van de onderbouwingen voor de afwijkende samples. Deze samples heb ik ook gevonden bij de eerste keer dat ik de PCA heb gemaakt. Dus de onderzoek naar waarom de samples afwijken kunnen nog steeds gebruikt worden. In de code heb ik wel een aantal variabelen aangepast zodat ik geen overlap heb met de oude PCA plots.

```{r code van Janine, warning=FALSE, message=FALSE}
pca_tools_matrix <- as.matrix(z_counts)

colnames(pca_tools_matrix) <- basename(colnames(pca_tools_matrix))

colnames(pca_tools_matrix) <- gsub(
  "_Aligned.sortedByCoord.out.bam",
  "",
  colnames(pca_tools_matrix)
)

pca_tools_metadata <- data.frame(
  sample = c(
    "SRR32635427", "SRR32635428", "SRR32635429",
    "SRR32635430", "SRR32635431", "SRR32635432",
    "SRR32635421", "SRR32635422", "SRR32635423",
    "SRR32635424", "SRR32635425", "SRR32635426",
    "SRR32635415", "SRR32635416", "SRR32635417",
    "SRR32635418", "SRR32635419", "SRR32635420"
  ),
  group = c(
    "0 h", "0 h", "0 h",
    "0 h", "0 h", "0 h",
    "4 h", "4 h", "4 h",
    "4 h", "4 h", "4 h",
    "24 h", "24 h", "24 h",
    "24 h", "24 h", "24 h"
  )
)

rownames(pca_tools_metadata) <- pca_tools_metadata$sample

pca_tools_metadata <- pca_tools_metadata[colnames(pca_tools_matrix), ]

pca_tools_result <- pca(
  mat = as.matrix(pca_tools_matrix),
  metadata = pca_tools_metadata,
  removeVar = 0.1
)

pca_tools_labels <- ifelse(
  rownames(pca_tools_metadata) %in% c("SRR32635416", "SRR32635421"),
  rownames(pca_tools_metadata),
  ""
)

biplot(
  pca_tools_result,
  colby = "group",
  lab = pca_tools_labels,
  labSize = 3
)
```


```{r}
pca_scores <- as.data.frame(pca_tools_result$rotated)

pca_scores$sample <- rownames(pca_scores)

max_PC1 <- pca_scores[which.max(pca_scores$PC1), ]
max_PC2 <- pca_scores[which.max(pca_scores$PC2), ]

cat("Sample met de hoogste PC1-score:", max_PC1$sample, "\n")

cat("Sample met de hoogste PC2-score:", max_PC2$sample)
```

Dit vertelt mij dat de sample met de hoogste PC1-score sample SRR32635416 is. De sample met de hoogste PC2-score is SRR32635421, die al eerder opviel en dit bevestigt nu dat deze sample afwijkt. 

Aangezien dit stukje code werkt op de output van de pca die constant verandert, komt het niet overeen met wat er besproken wordt als resultaat. Bij het kopje "18-05-2026" is de correct werkende PCA te zien waar deze resultaten in terug te vinden zijn. 

Er zijn nog enkele samples die enigsins buiten de groep liggen, maar omdat deze minder sterk afwijken en niet duidelijk naar voren kwamen in de eerdere analyses, worden deze niet afzonderlijk besproken. 

Om erachter te komen waarom deze 2 samples zo afwijkend zijn, ga ik eerst naar de counts.txt.summary kijken. Deze is samen met de count tabel gemaakt door FeatureCount. 

```{r}
fc_summary <- read.delim(
  "/students/2025-2026/Thema08/lipolyse/unpacked/transcriptomics/counts.txt.summary",
  check.names = FALSE
)

samples_interessant <- c(
  "SRR32635416_Aligned.sortedByCoord.out.bam",
  "SRR32635421_Aligned.sortedByCoord.out.bam"
)

categorieen_interessant <- c(
  "Assigned",
  "Unassigned_MultiMapping",
  "Unassigned_NoFeatures",
  "Unassigned_Ambiguity"
)

fc_interessant_kort <- fc_summary %>%
  filter(Status %in% categorieen_interessant) %>%
  select(Status, all_of(samples_interessant))

knitr::kable(fc_interessant_kort, row.names = FALSE)
```

Dit laat zien dat SRR32635416 28902814 assigned en 43622058 unassigned reads heeft. SRR32635421 heeft 17883374 assigned en 22911558 unassigned reads. Er lijkt verschil te zitten in de assigned vs unassigned reads voor elke sample, dus ik ga voor elke sample de percentage bepalen en deze plotten, waarbij ik de 2 opvallende samples uitlicht. 

```{r, echo=FALSE}
fc_long <- fc_summary %>%
  pivot_longer(
    cols = -Status,
    names_to = "sample",
    values_to = "reads"
  )

assignment_summary <- fc_long %>%
  mutate(
    assignment_group = ifelse(Status == "Assigned", "Assigned", "Unassigned")
  ) %>%
  group_by(sample, assignment_group) %>%
  summarise(reads = sum(reads), .groups = "drop") %>%
  group_by(sample) %>%
  mutate(
    total_reads = sum(reads),
    percentage = round(reads / total_reads * 100, 2)
  ) %>%
  ungroup()

unassigned_plot <- assignment_summary %>%
  filter(assignment_group == "Unassigned") %>%
  mutate(
    opvallend_sample = case_when(
      grepl("SRR32635416", sample) ~ "SRR32635416",
      grepl("SRR32635421", sample) ~ "SRR32635421",
      TRUE ~ "Overige samples"
    ),
    sample = reorder(sample, percentage)
  )

ggplot(unassigned_plot, aes(x = sample, y = percentage, fill = opvallend_sample)) +
  geom_col() +
  geom_text(
    aes(label = paste0(percentage, "%")),
    hjust = -0.1,
    size = 3
  ) +
  coord_flip() +
  theme_bw() +
  ylim(0, max(unassigned_plot$percentage) + 5) +
  labs(
    title = "Percentage unassigned reads per sample",
    x = "Sample",
    y = "Unassigned reads (%)",
    fill = "Sample"
  )
```

In deze grafiek is duidelijk te zien dat SRR32635416 het hoogste percentage unassigned reads heeft. Dit is waarschijnlijk de reden waarom dit sample afwijkend is ten opzichte van de rest. Voor de sample SRR32635421, die de hoogste PC1-score heeft, is het percentage unassigned reads vergelijkbaar met de overige samples. Dit is voor dit sample dus niet verklarend en zal er verder gezocht moeten worden. 

Omdat de percentage unassigned reads niks opvallends liet zien voor SRR32635421, ga ik nu kijken naar de afzonderlijke unassigned-categorieën. 

```{r, echo=FALSE}
fc_long_percentages <- fc_long %>%
  group_by(sample) %>%
  mutate(
    total_reads = sum(reads),
    percentage = round(reads / total_reads * 100, 2)
  ) %>%
  ungroup()


unassigned_detail_plot <- fc_long_percentages %>%
  filter(
    Status %in% c(
      "Unassigned_NoFeatures",
      "Unassigned_MultiMapping",
      "Unassigned_Ambiguity"
    )
  ) %>%
  mutate(
    groep = case_when(
      grepl("SRR32635416", sample) ~ "SRR32635416",
      grepl("SRR32635421", sample) ~ "SRR32635421",
      TRUE ~ "Overige samples"
    )
  )

ggplot() +
  geom_boxplot(
    data = unassigned_detail_plot %>% filter(groep == "Overige samples"),
    aes(x = reorder(Status, percentage), y = percentage),
    outlier.size = 1
  ) +
  geom_point(
    data = unassigned_detail_plot %>% filter(groep != "Overige samples"),
    aes(
      x = reorder(Status, percentage),
      y = percentage,
      color = groep
    ),
    size = 3
  ) +
  coord_flip() +
  theme_bw() +
  labs(
    title = "Unassigned-categorieën van opvallende samples",
    subtitle = "Punten = SRR32635416 en SRR32635421, boxplots = overige samples",
    x = "Unassigned-categorie",
    y = "Percentage van totaal aantal reads",
    color = "Sample"
  )
```

Deze plot toont aan dat er voor sample SRR32635421 nog geen duidelijke mogelijke reden is waarom deze zo afwijkt ten opzichte van de andere samples. De featureCounts-summary geeft dus geen duidelijke verklaring voor dit sample, dus er moet verder gekeken worden, bijvoorbeeld naar de aantal gedetecteerde genen per sample. Het was al duidelijk zichtbaar dat de sample SRR32635416 afweek van de overige samples, maar nu is het nog duidelijker dat het mogelijk de Unassigned_MultiMapping reads de oorzaak is van deze afwijking.

Ik kijk naar de aantal gedetecteerde genen per sample door de genen te tellen die een count hebben die hoger is dan 0. Een afwijkend laag of hoog aantal gedetecteerde genen kan wijzen op een verschil in expressieprofiel in het sample. 

```{r, echo=FALSE}
detected_genes <- data.frame(
  sample = colnames(count_matrix),
  detected_genes = colSums(count_matrix > 0)
) %>%
  mutate(
    opvallend_sample = case_when(
      grepl("SRR32635416", sample) ~ "SRR32635416",
      grepl("SRR32635421", sample) ~ "SRR32635421",
      TRUE ~ "Overige samples"
    ),
    sample = reorder(sample, detected_genes)
  )

ggplot(detected_genes, aes(x = sample, y = detected_genes, fill = opvallend_sample)) +
  geom_col() +
  geom_text(
    aes(label = detected_genes),
    hjust = -0.1,
    size = 3
  ) +
  coord_flip() +
  theme_bw() +
  ylim(0, max(detected_genes$detected_genes) + 3000) +
  labs(
    title = "Aantal gedetecteerde genen per sample",
    x = "Sample",
    y = "Aantal genen met count > 0",
    fill = "Sample"
  )
```

In deze grafiek is te zien dat SRR32635421 een redelijk laag aantal genen heeft met een count hoger dan 0, maar er zijn 3 samples die lager zijn, waaronder SRR32635416 die eerder al opviel. Dit is dus niet direct de oorzaak waarom sample SRR32635421 zo afwijkend is.

Ik ga nog een laatste check doen voor SRR32635421. Dit sample heeft wel een relatief laag aantal gedetecteerde samples, maar dit verklaart niet volledig waarom dit sample sterk afwijkt op PC1. Daarom ga ik kijken naar de genen die het meest bijdraagt aan PC1. PC1 verklaart de hoogste variatie in de dataset. Door de hoogste positieve en negatieve loadings van PC1 te bekijken, kan worden onderzocht welke genen vooral verantwoordelijk zijn voor de scheiding van samples langs deze component.

```{r}
pc1_loadings <- pca_tools_result$loadings[, 1]

names(pc1_loadings) <- rownames(pca_tools_result$loadings)

pc1_loadings <- pc1_loadings[is.finite(pc1_loadings)]

top_pc1_positive <- sort(pc1_loadings, decreasing = TRUE)[1:20]

top_pc1_negative <- sort(pc1_loadings, decreasing = FALSE)[1:20]

top_pc1_positive
top_pc1_negative
```

Dit zijn de genen die het meest bijdragen aan PC1, ik ga nu kijken of deze genen erg opvallen bij SRR32635421.

```{r, echo=FALSE}
top_pc1_genes_heatmap <- c(
  names(top_pc1_positive),
  names(top_pc1_negative)
)


# Heatmapwaarden uit z_counts of pca_tools_matrix halen
pc1_heatmap_values <- pca_tools_matrix[top_pc1_genes_heatmap, ]

# PCA-scores van samples ophalen
pca_tools_scores <- as.data.frame(pca_tools_result$rotated)
pca_tools_scores$sample <- rownames(pca_tools_scores)

# Samples ordenen op PC1
sample_order <- pca_tools_scores %>%
  arrange(PC1) %>%
  pull(sample)

pc1_heatmap_values <- pc1_heatmap_values[, sample_order]

sample_annotation <- data.frame(
  Sample = case_when(
    colnames(pc1_heatmap_values) == "SRR32635421" ~ "SRR32635421",
    colnames(pc1_heatmap_values) == "SRR32635416" ~ "SRR32635416",
    TRUE ~ "Overige samples"
  )
)

rownames(sample_annotation) <- colnames(pc1_heatmap_values)

pheatmap(
  pc1_heatmap_values,
  scale = "row",
  cluster_cols = FALSE,
  annotation_col = sample_annotation,
  fontsize_row = 6,
  fontsize_col = 7,
  angle_col = 45,
  main = "Top 40 genen die het sterkst bijdragen aan PC1"
)
```

In de heatmap is te zien dat SRR32635416 een ander expressiepatroon heeft dan de meeste andere samples voor de genen die het sterkst bijdragen aan PC1. Dit ondersteunt de PCA waarbij dit sample afweek van de rest. Vooral bij meerdere genen lijkt SRR32635416 relatief hogere expressiewaarden te hebben dan de meeste andere samples.

Omdat mijn PCA met de prcomp methode elke keer verandert bij het uitvoeren heb ik het op de handmatige manier geprobeerd. 
```{r}
# Data omzetten: samples in rijen, genen/features in kolommen
P <- t(z_counts)

# PCA berekenen via de samplematrix
R <- (P %*% t(P)) / (nrow(P) - 1)
eig <- eigen(R)

# Kleine negatieve eigenwaarden door afronding op 0 zetten
eig$values[eig$values < 0] <- 0

# PCA-scores van de samples berekenen
scores <- eig$vectors %*% diag(sqrt(eig$values))

# Percentage verklaarde variantie
var_pct <- round(eig$values / sum(eig$values) * 100, 1)

# Dataframe maken voor de plot
pca_plot <- data.frame(
  sample = rownames(P),
  PC1 = scores[, 1],
  PC2 = scores[, 2]
)

# Alleen SRR-nummer uit de sample-naam halen
pca_plot$sample_id <- sub("_Aligned.*", "", pca_plot$sample)

# Tijdstip toevoegen
pca_plot$tijdstip <- dplyr::case_when(
  pca_plot$sample_id %in% c(
    "SRR32635427", "SRR32635428", "SRR32635429",
    "SRR32635430", "SRR32635431", "SRR32635432"
  ) ~ "0 h",
  
  pca_plot$sample_id %in% c(
    "SRR32635421", "SRR32635422", "SRR32635423",
    "SRR32635424", "SRR32635425", "SRR32635426"
  ) ~ "4 h",
  
  pca_plot$sample_id %in% c(
    "SRR32635415", "SRR32635416", "SRR32635417",
    "SRR32635418", "SRR32635419", "SRR32635420"
  ) ~ "24 h"
)

# PCA-plot
ggplot(pca_plot, aes(x = PC1, y = PC2, color = tijdstip)) +
  geom_point(size = 3) +
  theme_bw() +
  labs(
    title = "PCA van gestandaardiseerde transcriptomicsdata",
    x = paste0("PC1 (", var_pct[1], "%)"),
    y = paste0("PC2 (", var_pct[2], "%)"),
    color = "Tijdstip"
  )
```

Deze verandert ook elke keer. Ik ga in de groep om hulp vragen.

#### 18-05-2026
Janine heeft mij geholpen met de oplossing voor de PCA. Bij haar was deze code stabiel, dus die heb ik overgenomen en onder het kopje "PCA verbetering" gezet. De output van deze PCA heb ik nodig voor analyse over de 2 afwijkende samples die ik eerder heb gevonden.

#### 22-05-2026

In de PCA van 18-05-2026 is te zien dat SRR32635416 juist het meeste afwijkt op PC1 en sample SRR32635421 het meest afwijkt op PC2. Het bewijst nogsteeds dat deze samples afwijkend zijn, maar alleen de heatmap op de genen die het meeste invloed hebben op PC1 wijst voor de sample SRR32635421 niks meer uit. Om deze reden voer ik het nog even snel op de genen die het meeste invloed hebben op PC2 uit, en dit vergelijk ik dan met de heatmap van de top 40 van de PC1 genen. 
```{r, echo=FALSE}
pc2_loadings <- pca_tools_result$loadings[, 2]

names(pc2_loadings) <- rownames(pca_tools_result$loadings)

pc2_loadings <- pc2_loadings[is.finite(pc2_loadings)]

top_pc2_positive <- sort(pc2_loadings, decreasing = TRUE)[1:20]
top_pc2_negative <- sort(pc2_loadings, decreasing = FALSE)[1:20]

top_pc2_genes_heatmap <- c(
  names(top_pc2_positive),
  names(top_pc2_negative)
)

top_pc2_genes_heatmap <- top_pc2_genes_heatmap[
  top_pc2_genes_heatmap %in% rownames(pca_tools_matrix)
]

pc2_heatmap_values <- pca_tools_matrix[top_pc2_genes_heatmap, , drop = FALSE]

pca_tools_scores <- as.data.frame(pca_tools_result$rotated)
pca_tools_scores$sample <- rownames(pca_tools_scores)

sample_order_pc2 <- pca_tools_scores %>%
  arrange(PC2) %>%
  pull(sample)

pc2_heatmap_values <- pc2_heatmap_values[, sample_order_pc2, drop = FALSE]

sample_annotation_pc2 <- data.frame(
  Sample = case_when(
    colnames(pc2_heatmap_values) == "SRR32635421" ~ "SRR32635421",
    colnames(pc2_heatmap_values) == "SRR32635416" ~ "SRR32635416",
    TRUE ~ "Overige samples"
  )
)

rownames(sample_annotation_pc2) <- colnames(pc2_heatmap_values)

pheatmap(
  pc2_heatmap_values,
  scale = "row",
  cluster_cols = FALSE,
  annotation_col = sample_annotation_pc2,
  fontsize_row = 6,
  fontsize_col = 7,
  angle_col = 45,
  main = "Top 40 genen die het sterkst bijdragen aan PC2"
)
```
De heatmap met de top 40 genen die het sterkst bijdragen aan PC2 is duidelijk te zien dat sample SRR32635421 een zeer afwijkend expressiepatroon heeft vergeleken met de andere samples. SRR32635415 is ook een opvallende sample qua expressiepatroon, maar die heeft een stuk minder genen die een hoge expressie hebben. Dit resultaat ondersteunt de reden waarom sample SRR32635421 afwijkt van de overige samples. 

Deze heatmap samen met de heatmap van de top 40 genen die het sterkst bijdragen aan PC1 geeft een duidelijke reden waarom sample SRR32635416 en SRR32635421 afwijken van de andere samples.

#### 26-05-2026

Vandaag maak ik de matrix zodat Joris de MOFA+ tool kan gebruiken. Voor uitleg over deze tool verwijs ik u graag naar zijn logboek. 

```{r}
transcriptomics_matrix <- as.matrix(z_counts)
mode(transcriptomics_matrix) <- "numeric"

colnames(transcriptomics_matrix) <- basename(colnames(transcriptomics_matrix))

colnames(transcriptomics_matrix) <- gsub(
  "_Aligned.sortedByCoord.out.bam",
  "",
  colnames(transcriptomics_matrix)
)
```

```{r, eval=FALSE}
write.csv(
  transcriptomics_matrix,
  "/students/2025-2026/Thema08/lipolyse/unpacked/transcriptomics/transcriptomics_matrix.csv"
)
```

```{r, echo=FALSE}
# Controles
matrix_controle <- data.frame(
  Controle = c(
    "Aantal genen/features",
    "Aantal samples",
    "Aantal NA-waarden",
    "Aantal NaN-waarden",
    "Aantal Inf-waarden",
    "Objectklasse",
    "Datamodus"
  ),
  Waarde = c(
    nrow(transcriptomics_matrix),
    ncol(transcriptomics_matrix),
    sum(is.na(transcriptomics_matrix)),
    sum(is.nan(transcriptomics_matrix)),
    sum(is.infinite(transcriptomics_matrix)),
    paste(class(transcriptomics_matrix), collapse = ", "),
    mode(transcriptomics_matrix)
  )
)

knitr::kable(matrix_controle, row.names = FALSE)
```

De feature matrix is nu gemaakt. Ik heb hiervoor de volledig genormaliseerde count data gebruikt die ik gemaakt heb bij mijn EDA. In de matrix heb ik de samples als kolommen en de features/genen als rijen opgeslagen. Tijdens het normaliseren ben ik een aantal features kwijt geraakt, dit zijn mogelijk genen die over elke sample dezelfde waarde heeft. Verder is ook te zien dat mijn matrix geen NA-, NaN-, of Inf-waarden bevatten. Dit betekent dat er geen waarden zijn die problemen gaan veroorzaken bij de verdere MOFA+-analyse.

#### 01-06-2026

Joris is bezig gegaan met het runnen van MOFA+. Dit is helaas niet gelukt. Daarom is afgelopen vrijdag (29-05-2026), in overleg met Marcel, een nieuw plan bedacht. Omdat MOFA+ niet succesvol kon worden uitgevoerd, is er gekozen om de transcriptomics- en proteomicsresultaten eerst apart te analyseren en daarna met elkaar te vergelijken.

Joris gaat DEG-lijsten maken van de transcriptomics, daarbij maakt hij een Venn-diagram en maakt een top 10 of 15 van de meest interessante genen. Nicole maakt de DAP-lijsten van de proteomics en maakt vervolgens een overlap lijst met de DAP-lijsten en DEG-lijsten. Ik ga me focussen op het uitvoeren van Metascape op de DEG- en DAP-lijsten, maak daaruit een top 15 van de overlappende genen/eiwitten en daaruit maak ik een aantal plotjes die in het artikel gebruikt kunnen worden. Janine gaat aan de hand van die top 15 overlappende genen/eiwitten opzoeken wat ze precies doen. 

Metascape is een webgebaseerde tool voor genlijst-annotatie en enrichment-analyse. Hiermee kunnen ingevoerde gen- of eiwitlijsten worden gekoppeld aan biologische processen, pathways en eiwitnetwerken. Deze tool is nuttig voor ons omdat de DEG- en DAP-lijsten op zichzelf aangeven welke genen of eiwitten significant veranderen, maar nog niet direct laten zien welke biologische processen hierbij betrokken worden. In dit project wordt Metascape daarom gebruikt om per DEG- en DAP-lijst de belangrijkste verrijkte pathways te bepalen en deze te vergelijken tussen de transcriptomics en proteomics.

Ik heb de DAP-lijsten al binnengehaald van de git-repository, dus deze ga ik eerst inlezen en controleren of het goed ingelezen wordt en controleren of er geen missende waarden aanwezig zijn. 

```{r dap-inlezen}
dap_all <- read.csv("../DAP_lists/All_DAPs_combined.csv")
```

```{r dap-controle, echo=FALSE}

dap_controle <- data.frame(
  Controle = c(
    "Aantal rijen",
    "Aantal kolommen",
    "Aantal missende waarden",
    "Aantal lege cellen"
  ),
  Waarde = c(
    nrow(dap_all),
    ncol(dap_all),
    sum(is.na(dap_all)),
    sum(dap_all == "", na.rm = TRUE)
  )
)

knitr::kable(dap_controle, row.names = FALSE)

```
De kolomnamen zijn zoals ik het heb verwacht, tijdens het overleg is er afgesproken dat in de DAP lijsten de gen ID, logFC, p-waarde en de aangepaste p-waarde geleverd zouden worden. Ik heb hier ook nog een kolom met 'comparison', deze kolom geeft aan uit welke tijdsvergelijking de DAP afkomstig is. Ook heb ik geen missende waarden of lege cellen in deze dataset. 

#### 02-06-2026

Ik heb ondertussen ook de DEG-lijsten gekregen van Joris, deze ga ik net als de DAP-lijsten bekijken en ik wil van beide nog even de statistsiche waarden bekijken om te bevestigen dat ze op dezelfde manier gefilterd zijn.

```{r degs inlezen}

deg_ab <- read.csv("/students/2025-2026/Thema08/lipolyse/unpacked/transcriptomics/significant_DEG_AB_rna.csv")

deg_ac <- read.csv("/students/2025-2026/Thema08/lipolyse/unpacked/transcriptomics/significant_DEG_AC_rna.csv")

deg_bc <- read.csv("/students/2025-2026/Thema08/lipolyse/unpacked/transcriptomics/significant_DEG_BC_rna.csv")

deg_4h_vs_0h <- deg_ab
deg_24h_vs_0h <- deg_ac
deg_24h_vs_4h <- deg_bc

```

```{r degs controle, echo=FALSE}
deg_controle <- data.frame(
  Vergelijking = c(
    "4h_vs_0h",
    "24h_vs_0h",
    "24h_vs_4h"
  ),
  Aantal_rijen = c(
    nrow(deg_4h_vs_0h),
    nrow(deg_24h_vs_0h),
    nrow(deg_24h_vs_4h)
  ),
  Aantal_kolommen = c(
    ncol(deg_4h_vs_0h),
    ncol(deg_24h_vs_0h),
    ncol(deg_24h_vs_4h)
  ),
  Aantal_missende_waarden = c(
    sum(is.na(deg_4h_vs_0h)),
    sum(is.na(deg_24h_vs_0h)),
    sum(is.na(deg_24h_vs_4h))
  ),
  Aantal_lege_cellen = c(
    sum(deg_4h_vs_0h == "", na.rm = TRUE),
    sum(deg_24h_vs_0h == "", na.rm = TRUE),
    sum(deg_24h_vs_4h == "", na.rm = TRUE)
  )
)

knitr::kable(deg_controle, row.names = FALSE)
```
In deze controle is te zien dat er geen missende waarden en lege cellen zijn. Ik heb 7 kolommen, zoals verwacht dus ik ga nu verder met de statistieke beoordeling van DEG- en DAP-lijsten.

```{r degs statistische controle}
deg_stat_controle <- data.frame(
  Vergelijking = c("4h_vs_0h", "24h_vs_0h", "24h_vs_4h"),
  
  Aantal_DEGs = c(
    nrow(deg_4h_vs_0h),
    nrow(deg_24h_vs_0h),
    nrow(deg_24h_vs_4h)
  ),
  
  Min_padj = c(
    min(deg_4h_vs_0h$padj, na.rm = TRUE),
    min(deg_24h_vs_0h$padj, na.rm = TRUE),
    min(deg_24h_vs_4h$padj, na.rm = TRUE)
  ),
  
  Max_padj = c(
    max(deg_4h_vs_0h$padj, na.rm = TRUE),
    max(deg_24h_vs_0h$padj, na.rm = TRUE),
    max(deg_24h_vs_4h$padj, na.rm = TRUE)
  ),
  
  Aantal_padj_boven_0.05 = c(
    sum(deg_4h_vs_0h$padj >= 0.05, na.rm = TRUE),
    sum(deg_24h_vs_0h$padj >= 0.05, na.rm = TRUE),
    sum(deg_24h_vs_4h$padj >= 0.05, na.rm = TRUE)
  ),
  
  Upregulated = c(
    sum(deg_4h_vs_0h$log2FoldChange > 0, na.rm = TRUE),
    sum(deg_24h_vs_0h$log2FoldChange > 0, na.rm = TRUE),
    sum(deg_24h_vs_4h$log2FoldChange > 0, na.rm = TRUE)
  ),
  
  Downregulated = c(
    sum(deg_4h_vs_0h$log2FoldChange < 0, na.rm = TRUE),
    sum(deg_24h_vs_0h$log2FoldChange < 0, na.rm = TRUE),
    sum(deg_24h_vs_4h$log2FoldChange < 0, na.rm = TRUE)
  ),
  
  Min_log2FC = c(
    min(deg_4h_vs_0h$log2FoldChange, na.rm = TRUE),
    min(deg_24h_vs_0h$log2FoldChange, na.rm = TRUE),
    min(deg_24h_vs_4h$log2FoldChange, na.rm = TRUE)
  ),
  
  Mediaan_log2FC = c(
    median(deg_4h_vs_0h$log2FoldChange, na.rm = TRUE),
    median(deg_24h_vs_0h$log2FoldChange, na.rm = TRUE),
    median(deg_24h_vs_4h$log2FoldChange, na.rm = TRUE)
  ),
  
  Max_log2FC = c(
    max(deg_4h_vs_0h$log2FoldChange, na.rm = TRUE),
    max(deg_24h_vs_0h$log2FoldChange, na.rm = TRUE),
    max(deg_24h_vs_4h$log2FoldChange, na.rm = TRUE)
  )
)

knitr::kable(deg_stat_controle, row.names = FALSE, digits = 4)

```


```{r dap-lijsten inlezen, include=FALSE}

dap_4h_vs_0h <- read.csv("../DAP_lists/DAP_4h_vs_0h.csv")
dap_24h_vs_0h <- read.csv("../DAP_lists/DAP_24h_vs_0h.csv")
dap_24h_vs_4h <- read.csv("../DAP_lists/DAP_24h_vs_4h.csv")

```


```{r dap statistische controle, echo=FALSE}

dap_stat_controle <- data.frame(
  Vergelijking = c("4h_vs_0h", "24h_vs_0h", "24h_vs_4h"),
  
  Aantal_DAPs = c(
    nrow(dap_4h_vs_0h),
    nrow(dap_24h_vs_0h),
    nrow(dap_24h_vs_4h)
  ),
  
  Min_adj_P_Val = c(
    min(dap_4h_vs_0h$adj.P.Val, na.rm = TRUE),
    min(dap_24h_vs_0h$adj.P.Val, na.rm = TRUE),
    min(dap_24h_vs_4h$adj.P.Val, na.rm = TRUE)
  ),
  
  Max_adj_P_Val = c(
    max(dap_4h_vs_0h$adj.P.Val, na.rm = TRUE),
    max(dap_24h_vs_0h$adj.P.Val, na.rm = TRUE),
    max(dap_24h_vs_4h$adj.P.Val, na.rm = TRUE)
  ),
  
  Aantal_adj_P_Val_boven_0.05 = c(
    sum(dap_4h_vs_0h$adj.P.Val >= 0.05, na.rm = TRUE),
    sum(dap_24h_vs_0h$adj.P.Val >= 0.05, na.rm = TRUE),
    sum(dap_24h_vs_4h$adj.P.Val >= 0.05, na.rm = TRUE)
  ),
  
  Upregulated = c(
    sum(dap_4h_vs_0h$logFC > 0, na.rm = TRUE),
    sum(dap_24h_vs_0h$logFC > 0, na.rm = TRUE),
    sum(dap_24h_vs_4h$logFC > 0, na.rm = TRUE)
  ),
  
  Downregulated = c(
    sum(dap_4h_vs_0h$logFC < 0, na.rm = TRUE),
    sum(dap_24h_vs_0h$logFC < 0, na.rm = TRUE),
    sum(dap_24h_vs_4h$logFC < 0, na.rm = TRUE)
  ),
  
  Min_logFC = c(
    min(dap_4h_vs_0h$logFC, na.rm = TRUE),
    min(dap_24h_vs_0h$logFC, na.rm = TRUE),
    min(dap_24h_vs_4h$logFC, na.rm = TRUE)
  ),
  
  Mediaan_logFC = c(
    median(dap_4h_vs_0h$logFC, na.rm = TRUE),
    median(dap_24h_vs_0h$logFC, na.rm = TRUE),
    median(dap_24h_vs_4h$logFC, na.rm = TRUE)
  ),
  
  Max_logFC = c(
    max(dap_4h_vs_0h$logFC, na.rm = TRUE),
    max(dap_24h_vs_0h$logFC, na.rm = TRUE),
    max(dap_24h_vs_4h$logFC, na.rm = TRUE)
  )
)

knitr::kable(dap_stat_controle, row.names = FALSE, digits = 4)

```

De statistische toetsen vertellen mij dat de DEG-lijsten wel gefilterd zijn op padj <0.05, maar de DAP-lijsten zijn niet gefilterd op de padj <0.05. Deze filter ga ik nog even toepassen en de output ga ik dan wegschrijven. De log fold change is niet gefilterd, misschien dat er met die filter teveel data verloren gaat. Ik ga dus eerst de lijsten proberen die op padj<0.05 gefilterd zijn in Metascape, als die data teveel is ga ik de log fold change filteren.

```{r dap-lijsten filteren, eval=FALSE}

dap_4h_vs_0h_sig <- dap_4h_vs_0h %>%
  filter(adj.P.Val < 0.05)

dap_24h_vs_0h_sig <- dap_24h_vs_0h %>%
  filter(adj.P.Val < 0.05)

dap_24h_vs_4h_sig <- dap_24h_vs_4h %>%
  filter(adj.P.Val < 0.05)

write.csv(
  dap_4h_vs_0h_sig, "/students/2025-2026/Thema08/lipolyse/unpacked/proteomics/significant_DAP_4h_vs_0h.csv", row.names = FALSE
)

write.csv(
  dap_24h_vs_0h_sig, "/students/2025-2026/Thema08/lipolyse/unpacked/proteomics/significant_DAP_24h_vs_0h.csv", row.names = FALSE
)

write.csv(
  dap_24h_vs_4h_sig, "/students/2025-2026/Thema08/lipolyse/unpacked/proteomics/significant_DAP_24h_vs_4h.csv", row.names = FALSE
)

```

```{r dap-filter controle, echo=FALSE}

dap_filter_controle <- data.frame(
  Vergelijking = c("4h_vs_0h", "24h_vs_0h", "24h_vs_4h"),
  Voor_filtering = c(
    nrow(dap_4h_vs_0h),
    nrow(dap_24h_vs_0h),
    nrow(dap_24h_vs_4h)
  ),
  Na_filtering = c(
    nrow(dap_4h_vs_0h_sig),
    nrow(dap_24h_vs_0h_sig),
    nrow(dap_24h_vs_4h_sig)
  ),
  Max_adj_P_Val_na_filtering = c(
    max(dap_4h_vs_0h_sig$adj.P.Val, na.rm = TRUE),
    max(dap_24h_vs_0h_sig$adj.P.Val, na.rm = TRUE),
    max(dap_24h_vs_4h_sig$adj.P.Val, na.rm = TRUE)
  )
)

knitr::kable(dap_filter_controle, row.names = FALSE, digits = 4)

```

Na het toepassen van de filter padj <0.05 is te zien dat er aanzienlijk minder rijen aanwezig zijn in de DAP-lijsten. Aan de max_adj_P_val_na_filtering is te zien dat er goed gefilterd is. De gefilterde DAP- en DEG-lijsten ga ik uploaden naar Metascape per groep en met die uitkomst ga ik verder werken. 

Voor Metascape moet ik per groep 1 bestand maken waarbij de DAP-genen en de DEG-genen per kolom in staan. 

```{r metascape-files maken, eval=FALSE}

make_metascape_file <- function(dap_genes, deg_genes, output_file) {
  
  dap_genes <- unique(na.omit(dap_genes))
  deg_genes <- unique(na.omit(deg_genes))
  
  max_length <- max(length(dap_genes), length(deg_genes))
  
  dap_genes <- c(dap_genes, rep(NA, max_length - length(dap_genes)))
  deg_genes <- c(deg_genes, rep(NA, max_length - length(deg_genes)))
  
  metascape_input <- data.frame(
    DAP = dap_genes,
    DEG = deg_genes
  )
  
  write.csv(
    metascape_input,
    output_file,
    row.names = FALSE,
    quote = FALSE,
    na = ""
  )
}

make_metascape_file(
  dap_genes = dap_4h_vs_0h_sig$ensembl_gene_id,
  deg_genes = deg_4h_vs_0h$Gene,
  output_file = "/students/2025-2026/Thema08/lipolyse/unpacked/Metascape_input/Metascape_4h_vs_0h.csv"
)

make_metascape_file(
  dap_genes = dap_24h_vs_0h_sig$ensembl_gene_id,
  deg_genes = deg_24h_vs_0h$Gene,
  output_file = "/students/2025-2026/Thema08/lipolyse/unpacked/Metascape_input/Metascape_24h_vs_0h.csv"
)

make_metascape_file(
  dap_genes = dap_24h_vs_4h_sig$ensembl_gene_id,
  deg_genes = deg_24h_vs_4h$Gene,
  output_file = "/students/2025-2026/Thema08/lipolyse/unpacked/Metascape_input/Metascape_24h_vs_4h.csv"
)

```

```{r metascape lijst controle}

metalijst_4h_vs_0h <- read.csv("/students/2025-2026/Thema08/lipolyse/unpacked/Metascape_input/Metascape_4h_vs_0h.csv")
metalijst_24h_vs_0h <- read.csv("/students/2025-2026/Thema08/lipolyse/unpacked/Metascape_input/Metascape_24h_vs_0h.csv")
metalijst_24h_vs_4h <- read.csv("/students/2025-2026/Thema08/lipolyse/unpacked/Metascape_input/Metascape_24h_vs_4h.csv")

head(metalijst_4h_vs_0h)
head(metalijst_24h_vs_0h)
head(metalijst_24h_vs_4h)

```

De lijsten zijn goed gemaakt, dus ik ga metascape uitvoeren op de website. Deze lijsten pakt Metascape perfect, dus ik ga niet verder filteren op de log fold change. 

Metascape is nu uitgevoerd, ik heb de resultaten opgeslagen en alvast kort bekeken. Het viel me op dat er veel housekeeping pathways in zitten. Housekeeping pathways hebben te maken met algemene cellulaire processen en zijn niet direct specifiek voor lipolyse. Hier ga ik morgen mee verder om te kijken of deze eruit gehaald kunnen worden zodat we alleen de pathways hebben waarin we  geïnteresseerd zijn. 

#### 03-06-2026

Gisteren heb ik de Metascape gerund en die resultaten opgeslagen. Ik ga nu kijken of ik de housekeeping pathways kan filteren. Het doel hiervan is om de uiteindelijke grafieken vooral pathways te tonen die inhoudelijk relevant zijn voor lipolyse, vetzuurmetabolisme, mitochondriale activiteit en thermogenese. Ik heb specifiek voor deze pathways gekozen omdat deze processen gelinkt zijn aan lipolyse. 

```{r metascape resultaten inlezen}

metascape_4h_vs_0h <- read_excel(
  "/students/2025-2026/Thema08/lipolyse/unpacked/Metascape_output/comparison_4h_vs_0h/metascape_result_4hvs0h.xlsx",
  sheet = "Enrichment"
)

metascape_24h_vs_0h <- read_excel(
  "/students/2025-2026/Thema08/lipolyse/unpacked/Metascape_output/comparison_24h_vs_0h/metascape_result_24hvs0h.xlsx",
  sheet = "Enrichment"
)

metascape_24h_vs_4h <- read_excel(
  "/students/2025-2026/Thema08/lipolyse/unpacked/Metascape_output/comparison_24h_vs_4h/metascape_result_24hvs4h.xlsx",
  sheet = "Enrichment"
)

```

```{r metascape check}
metascape_controle <- data.frame(
  Vergelijking = c("4h_vs_0h", "24h_vs_0h", "24h_vs_4h"),
  Aantal_rijen = c(
    nrow(metascape_4h_vs_0h),
    nrow(metascape_24h_vs_0h),
    nrow(metascape_24h_vs_4h)
  ),
  Aantal_kolommen = c(
    ncol(metascape_4h_vs_0h),
    ncol(metascape_24h_vs_0h),
    ncol(metascape_24h_vs_4h)
  ),
  Kolomnamen = c(
    paste(colnames(metascape_4h_vs_0h), collapse = ", "),
    paste(colnames(metascape_24h_vs_0h), collapse = ", "),
    paste(colnames(metascape_24h_vs_4h), collapse = ", ")
  )
)

knitr::kable(metascape_controle, row.names = FALSE)
```

Ik heb een controle uitgevoerd of het goed ingelezen is omdat ik nog nooit met meerdere tabbladen in een excel bestand heb gewerkt. Het aantal kolommen en de kolomnamen vertellen mij dat het goed is ingelezen. Ik heb eerst groupID nodig om te filteren, en even later heb ik ook de Log(q-value) nodig.

In Metascape worden de pathways geclusterd en er bestaat veel overlap in de termen die Metascape geeft. Daarom ga ik eerst op 'summary' filteren. Ook deze ga ik weer controleren om te kijken of alle summaries apart worden opgeslagen.

```{r filteren op summary}
metascape_summary_4h_vs_0h <- metascape_4h_vs_0h %>%
  filter(grepl("Summary", GroupID))

metascape_summary_24h_vs_0h <- metascape_24h_vs_0h %>%
  filter(grepl("Summary", GroupID))

metascape_summary_24h_vs_4h <- metascape_24h_vs_4h %>%
  filter(grepl("Summary", GroupID))
```

```{r summary controle}
summary_controle <- data.frame(
  Vergelijking = c("4h_vs_0h", "24h_vs_0h", "24h_vs_4h"),
  Aantal_termen_totaal = c(
    nrow(metascape_4h_vs_0h),
    nrow(metascape_24h_vs_0h),
    nrow(metascape_24h_vs_4h)
  ),
  Aantal_summary_termen = c(
    nrow(metascape_summary_4h_vs_0h),
    nrow(metascape_summary_24h_vs_0h),
    nrow(metascape_summary_24h_vs_4h)
  )
)

knitr::kable(summary_controle, row.names = FALSE)
```

Na het filteren op summary is te zien dat er voor elke vergelijking 20 summaries aanwezig zijn.

Nu deze opgeslagen zijn, ga ik eerst relevante en housekeeping zoektermen opslaan. Dit doe ik om het filteren straks gemakkelijker te maken.

```{r zoektermen opslaan}
relevante_termen <- c(
  "lipid",
  "lipolysis",
  "fatty acid",
  "fatty acid oxidation",
  "beta-oxidation",
  "PPAR",
  "adip",
  "thermogenesis",
  "mitochond",
  "oxidative phosphorylation",
  "electron transport",
  "TCA",
  "tricarboxylic",
  "cholesterol",
  "peroxisome"
)

housekeeping_termen <- c(
  "ribosome",
  "translation",
  "rRNA",
  "RNA processing",
  "spliceosome",
  "protein folding",
  "proteasome",
  "cell cycle",
  "DNA replication",
  "mitotic",
  "chromosome",
  "nucleotide",
  "purine",
  "pyrimidine",
  "amino acid biosynthesis"
)
```

Voor ik alle housekeeping pathways ga verwijderen, wil ik eerst kijken hoeveel housekeeping pathways en lipolyse gerelateerde pathways aanwezig zijn. Dit doe ik door te markeren en daarna voer ik weer een controle stap uit die kijkt hoeveel er van elk is.

```{r relevante en housekeeping pathways markeren}
metascape_summary_4h_vs_0h <- metascape_summary_4h_vs_0h %>%
  mutate(
    relevant_lipolyse = grepl(
      paste(relevante_termen, collapse = "|"),
      Description,
      ignore.case = TRUE
    ),
    housekeeping = grepl(
      paste(housekeeping_termen, collapse = "|"),
      Description,
      ignore.case = TRUE
    )
  )

metascape_summary_24h_vs_0h <- metascape_summary_24h_vs_0h %>%
  mutate(
    relevant_lipolyse = grepl(
      paste(relevante_termen, collapse = "|"),
      Description,
      ignore.case = TRUE
    ),
    housekeeping = grepl(
      paste(housekeeping_termen, collapse = "|"),
      Description,
      ignore.case = TRUE
    )
  )

metascape_summary_24h_vs_4h <- metascape_summary_24h_vs_4h %>%
  mutate(
    relevant_lipolyse = grepl(
      paste(relevante_termen, collapse = "|"),
      Description,
      ignore.case = TRUE
    ),
    housekeeping = grepl(
      paste(housekeeping_termen, collapse = "|"),
      Description,
      ignore.case = TRUE
    )
  )
```

```{r filter controle termen}
filter_controle <- data.frame(
  Vergelijking = c("4h_vs_0h", "24h_vs_0h", "24h_vs_4h"),
  
  Summary_termen = c(
    nrow(metascape_summary_4h_vs_0h),
    nrow(metascape_summary_24h_vs_0h),
    nrow(metascape_summary_24h_vs_4h)
  ),
  
  Relevante_lipolyse_termen = c(
    sum(metascape_summary_4h_vs_0h$relevant_lipolyse),
    sum(metascape_summary_24h_vs_0h$relevant_lipolyse),
    sum(metascape_summary_24h_vs_4h$relevant_lipolyse)
  ),
  
  Housekeeping_termen = c(
    sum(metascape_summary_4h_vs_0h$housekeeping),
    sum(metascape_summary_24h_vs_0h$housekeeping),
    sum(metascape_summary_24h_vs_4h$housekeeping)
  )
)

knitr::kable(filter_controle, row.names = FALSE)
```

Ik had verwacht dat de 20 in ieder geval verdeeld zouden worden over de lipolyse termen en housekeeping termen. Ik ga eerst even kijken naar de relevante termen en daarna kan ik nog beslissen of ik de relevante termen wil aanpassen.

```{r relevante termen bekijken}
relevante_metascape_termen <- bind_rows(
  metascape_summary_4h_vs_0h %>%
    filter(relevant_lipolyse == TRUE) %>%
    mutate(Vergelijking = "4h_vs_0h"),
  
  metascape_summary_24h_vs_0h %>%
    filter(relevant_lipolyse == TRUE) %>%
    mutate(Vergelijking = "24h_vs_0h"),
  
  metascape_summary_24h_vs_4h %>%
    filter(relevant_lipolyse == TRUE) %>%
    mutate(Vergelijking = "24h_vs_4h")
)

relevante_metascape_termen <- relevante_metascape_termen %>%
  select(
    Vergelijking,
    Category,
    Description,
    LogP,
    `Log(q-value)`
  ) %>%
  arrange(Vergelijking, `Log(q-value)`)

knitr::kable(relevante_metascape_termen, row.names = FALSE, digits = 3)
```

Dit zijn niet de pathways die direct betrokken zijn bij de lipolyse. Ik ga nu alle pathways bekijken die onder summary vallen en kijken of daar interessante pathways tussen zitten. Mochten die aanwezig zijn, dan breidt ik de lijst uit.

```{r termen check complete summary}
summary_terms_all <- bind_rows(
  metascape_summary_4h_vs_0h %>%
    mutate(Vergelijking = "4h_vs_0h"),
  metascape_summary_24h_vs_0h %>%
    mutate(Vergelijking = "24h_vs_0h"),
  metascape_summary_24h_vs_4h %>%
    mutate(Vergelijking = "24h_vs_4h")
) %>%
  select(
    Vergelijking,
    Category,
    Description,
    LogP,
    `Log(q-value)`,
    relevant_lipolyse,
    housekeeping
  ) %>%
  arrange(Vergelijking, `Log(q-value)`)

knitr::kable(summary_terms_all, row.names = FALSE, digits = 3)
```

In de lijst zijn nog veel pathways te zien die wel relevant zijn bij lipolyse. Ze horen er niet letterlijk bij maar tonen wel aan of er lipolyse plaats vindt. Daarom heb ik een tweede lijst gemaakt met relevante termen die uitgebreider is dan de eerste.

```{r, uitgebreide lijst relevante termen}
uitgebreide_relevante_termen <- c(
  "lipid",
  "lipolysis",
  "fatty acid",
  "fatty acyl",
  "acyl-CoA",
  "acyl",
  "beta-oxidation",
  "oxidation of fatty acids",
  "carnitine",
  "glycerolipid",
  "triglyceride",
  "cholesterol",
  "adip",
  "brown fat",
  "thermogenesis",
  "PPAR",
  "mitochond",
  "oxidative phosphorylation",
  "electron transport",
  "respiratory chain",
  "TCA",
  "tricarboxylic",
  "citrate cycle",
  "citric acid cycle",
  "carbon metabolism",
  "pyruvate",
  "generation of precursor metabolites and energy",
  "monocarboxylic acid metabolic process",
  "dicarboxylic acid metabolic process",
  "small molecule catabolic process",
  "small molecule biosynthetic process",
  "amino acid metabolic process",
  "valine, leucine and isoleucine degradation",
  "NADP",
  "peroxisome"
)
```

```{r uitgebreide gemarkeerde lijst}
metascape_summary_4h_vs_0h_uitgebreid <- metascape_summary_4h_vs_0h %>%
  mutate(
    relevant_thermogenese_metabolisme_uitgebreid = grepl(
      paste(uitgebreide_relevante_termen, collapse = "|"),
      Description,
      ignore.case = TRUE
    )
  )

metascape_summary_24h_vs_0h_uitgebreid <- metascape_summary_24h_vs_0h %>%
  mutate(
    relevant_thermogenese_metabolisme_uitgebreid = grepl(
      paste(uitgebreide_relevante_termen, collapse = "|"),
      Description,
      ignore.case = TRUE
    )
  )

metascape_summary_24h_vs_4h_uitgebreid <- metascape_summary_24h_vs_4h %>%
  mutate(
    relevant_thermogenese_metabolisme_uitgebreid = grepl(
      paste(uitgebreide_relevante_termen, collapse = "|"),
      Description,
      ignore.case = TRUE
    )
  )
```

```{r controle uitgebreide gemarkeerde lijst}
filter_controle_uitgebreid <- data.frame(
  Vergelijking = c("4h_vs_0h", "24h_vs_0h", "24h_vs_4h"),
  
  Summary_termen = c(
    nrow(metascape_summary_4h_vs_0h_uitgebreid),
    nrow(metascape_summary_24h_vs_0h_uitgebreid),
    nrow(metascape_summary_24h_vs_4h_uitgebreid)
  ),
  
  Uitgebreide_relevante_termen = c(
    sum(metascape_summary_4h_vs_0h_uitgebreid$relevant_thermogenese_metabolisme_uitgebreid),
    sum(metascape_summary_24h_vs_0h_uitgebreid$relevant_thermogenese_metabolisme_uitgebreid),
    sum(metascape_summary_24h_vs_4h_uitgebreid$relevant_thermogenese_metabolisme_uitgebreid)
  ),
  
  Housekeeping_termen = c(
    sum(metascape_summary_4h_vs_0h_uitgebreid$housekeeping),
    sum(metascape_summary_24h_vs_0h_uitgebreid$housekeeping),
    sum(metascape_summary_24h_vs_4h_uitgebreid$housekeeping)
  )
)

knitr::kable(filter_controle_uitgebreid, row.names = FALSE)
```

Na het uitbreiden van de relevante termenlijst heb ik aanzienlijk meer relevante pathways teruggekregen. Wat me wel opvalt is dat de overgang van 0h naar 4h en van 4h naar 24h meer relevante pathways heeft in vergelijking tot de overgang van 0h naar 24h. Dit kan betekenen dat de veranderingen gefaseerd verlopen, dus dat er veel tussen 0h en 4h gebeurt en tussen 4h en 24h. Maar dat de processen misschien weer afgevlakt, verschoven of minder duidelijk zijn tussen 0h en 24h. Maar het kan ook komen door mijn selectie, want ze hebben allemaal 20 summary-pathways. Ook naar de verhoudingen gekeken, bevat de vergelijking 24h vs 0h meer housekeeping pathways. Ik ga eerst even de relevante pathways bekijken en dan ga ik plotjes maken voor in het artikel.

```{r relevante pathways bekijken}
relevante_pathways_tabel <- bind_rows(
  metascape_summary_4h_vs_0h_uitgebreid %>%
    filter(relevant_thermogenese_metabolisme_uitgebreid == TRUE) %>%
    mutate(Vergelijking = "4h_vs_0h"),
  
  metascape_summary_24h_vs_0h_uitgebreid %>%
    filter(relevant_thermogenese_metabolisme_uitgebreid == TRUE) %>%
    mutate(Vergelijking = "24h_vs_0h"),
  
  metascape_summary_24h_vs_4h_uitgebreid %>%
    filter(relevant_thermogenese_metabolisme_uitgebreid == TRUE) %>%
    mutate(Vergelijking = "24h_vs_4h")
) %>%
  select(
    Vergelijking,
    Category,
    Description,
    LogP,
    `Log(q-value)`
  ) %>%
  arrange(Vergelijking, `Log(q-value)`)

knitr::kable(
  relevante_pathways_tabel,
  row.names = FALSE,
  digits = 3
)
```

Ik ga deze tabel wegschrijven in een .csv bestand zodat andere groepsgenoten er ook bij kunnen.

```{r resultaten wegschrijven}
write.csv(
  relevante_pathways_tabel,
  "/students/2025-2026/Thema08/lipolyse/unpacked/Metascape_output/relevante_pathways_metascape.csv",
  row.names = FALSE
)
```

Ik ga een heatmap maken waar het per pathway en vergelijking is welke pathway waar het sterkst tot uiting komt en een venn-diagram zodat het duidelijk is welke pathways bij welke vergelijkingen overlappen. 

```{r heatmap relevante pathways, echo=FALSE, fig.width=8, fig.height=5}
heatmap_data <- relevante_pathways_tabel %>%
  mutate(
    enrichment_score = -`Log(q-value)`,
    Description_clean = gsub(" - Mus musculus \\(house mouse\\)", "", Description),
    pathway_groep = case_when(
      grepl("mitochond|TCA|citrate cycle|electron transport|oxidative phosphorylation|protein localization to mitochondrion",
            Description_clean, ignore.case = TRUE) ~ "Mitochondriaal / energie",
      
      grepl("carbon metabolism|pyruvate|precursor metabolites|NADP",
            Description_clean, ignore.case = TRUE) ~ "Energiemetabolisme",
      
      grepl("amino acid|valine|leucine|isoleucine|tRNA aminoacylation",
            Description_clean, ignore.case = TRUE) ~ "Aminozuurmetabolisme",
      
      grepl("acyl-CoA|dicarboxylic acid|monocarboxylic acid|fatty acid|lipid",
            Description_clean, ignore.case = TRUE) ~ "Vetzuur / organisch zuur metabolisme",
      
      grepl("small molecule",
            Description_clean, ignore.case = TRUE) ~ "Small molecule metabolism",
      
      TRUE ~ "Overig"
    )
  ) %>%
  select(Description_clean, pathway_groep, Vergelijking, enrichment_score) %>%
  pivot_wider(
    names_from = Vergelijking,
    values_from = enrichment_score,
    values_fill = 0
  ) %>%
  arrange(
    factor(
      pathway_groep,
      levels = c(
        "Vetzuur / organisch zuur metabolisme",
        "Mitochondriaal / energie",
        "Energiemetabolisme",
        "Aminozuurmetabolisme",
        "Small molecule metabolism",
        "Overig"
      )
    ),
    Description_clean
  )

heatmap_matrix <- as.data.frame(heatmap_data)
rownames(heatmap_matrix) <- heatmap_matrix$Description_clean

row_annotation <- data.frame(
  Categorie = heatmap_matrix$pathway_groep
)
rownames(row_annotation) <- rownames(heatmap_matrix)

heatmap_matrix <- heatmap_matrix %>%
  select(-Description_clean, -pathway_groep)

pheatmap(
  as.matrix(heatmap_matrix),
  cluster_rows = FALSE,
  cluster_cols = FALSE,
  annotation_row = row_annotation,
  main = "Relevante pathways per vergelijking",
  angle_col = 90,
  fontsize_row = 7,
  fontsize_col = 9,
  fontsize = 9,
  treeheight_row = 0,
  treeheight_col = 0,
  legend = TRUE,
  annotation_legend = TRUE
)
```
Er is duidelijk verschil te zien in de heatmap bij welke pathways waar tot expressie komen en hoe hoog. Er zijn pathways die niet bij elke vergelijking aanwezig, deze zijn gemarkeerd als 0 en opgevuld met donkerblauw. Verder heb ik categorieën gemaakt voor de pathways zodat het duidelijk is welke pathway bij welke groep hoort. Dit maakt het in het artikel ook makkelijker om te filteren op belang en welke pathway specifiek voor lipolyse is en welke bij bijvoorbeeld energiemetabolisme hoort. 

Ik maak nog even een venn diagram om een mooi overzicht te krijgen welke pathway bij welke vergelijking hoort en of de pathways bij meer vergelijkingen voorkomen.

```{r venn voorbereiding}

pathways_4h_vs_0h <- relevante_pathways_tabel %>%
  filter(Vergelijking == "4h_vs_0h") %>%
  pull(Description) %>%
  unique()

pathways_24h_vs_0h <- relevante_pathways_tabel %>%
  filter(Vergelijking == "24h_vs_0h") %>%
  pull(Description) %>%
  unique()

pathways_24h_vs_4h <- relevante_pathways_tabel %>%
  filter(Vergelijking == "24h_vs_4h") %>%
  pull(Description) %>%
  unique()

```

```{r venn plot}
pathway_overlap_plot <- relevante_pathways_tabel %>%
  mutate(
    Pathway = gsub(" - Mus musculus \\(house mouse\\)", "", Description),
    aanwezig = TRUE,
    pathway_groep = case_when(
      grepl(
        "mitochond|TCA|citrate cycle|electron transport|oxidative phosphorylation|protein localization to mitochondrion",
        Pathway,
        ignore.case = TRUE
      ) ~ "Mitochondriaal / energie",
      
      grepl(
        "carbon metabolism|pyruvate|precursor metabolites|NADP",
        Pathway,
        ignore.case = TRUE
      ) ~ "Energiemetabolisme",
      
      grepl(
        "amino acid|valine|leucine|isoleucine|tRNA aminoacylation",
        Pathway,
        ignore.case = TRUE
      ) ~ "Aminozuurmetabolisme",
      
      grepl(
        "acyl-CoA|dicarboxylic acid|monocarboxylic acid|fatty acid|lipid",
        Pathway,
        ignore.case = TRUE
      ) ~ "Vetzuur / organisch zuur metabolisme",
      
      grepl(
        "small molecule",
        Pathway,
        ignore.case = TRUE
      ) ~ "Small molecule metabolism",
      
      TRUE ~ "Overig"
    )
  ) %>%
  select(Pathway, pathway_groep, Vergelijking, aanwezig) %>%
  distinct()

pathway_info <- pathway_overlap_plot %>%
  distinct(Pathway, pathway_groep)

pathway_overlap_plot <- pathway_overlap_plot %>%
  select(Pathway, Vergelijking, aanwezig) %>%
  complete(
    Pathway,
    Vergelijking = c("4h_vs_0h", "24h_vs_0h", "24h_vs_4h"),
    fill = list(aanwezig = FALSE)
  ) %>%
  left_join(pathway_info, by = "Pathway") %>%
  group_by(Pathway) %>%
  mutate(
    aantal_vergelijkingen = sum(aanwezig)
  ) %>%
  ungroup() %>%
  arrange(
    factor(
      pathway_groep,
      levels = c(
        "Vetzuur / organisch zuur metabolisme",
        "Mitochondriaal / energie",
        "Energiemetabolisme",
        "Aminozuurmetabolisme",
        "Small molecule metabolism",
        "Overig"
      )
    ),
    desc(aantal_vergelijkingen),
    Pathway
  ) %>%
  mutate(
    Pathway = factor(Pathway, levels = rev(unique(Pathway))),
    Vergelijking = factor(
      Vergelijking,
      levels = c("4h_vs_0h", "24h_vs_0h", "24h_vs_4h")
    ),
    x_num = as.numeric(Vergelijking)
  )

pathway_lijnen <- pathway_overlap_plot %>%
  filter(aanwezig == TRUE) %>%
  group_by(Pathway) %>%
  summarise(
    x_start = min(x_num),
    x_end = max(x_num),
    .groups = "drop"
  ) %>%
  filter(x_start != x_end)

categorie_balk <- pathway_overlap_plot %>%
  distinct(Pathway, pathway_groep)

ggplot(pathway_overlap_plot, aes(x = x_num, y = Pathway)) +
  geom_tile(
    data = categorie_balk,
    aes(x = 0.55, y = Pathway, fill = pathway_groep),
    inherit.aes = FALSE,
    width = 0.12,
    height = 0.8
  ) +
  geom_segment(
    data = pathway_lijnen,
    aes(
      x = x_start,
      xend = x_end,
      y = Pathway,
      yend = Pathway
    ),
    inherit.aes = FALSE,
    linewidth = 0.6
  ) +
  geom_point(
    aes(color = aanwezig),
    size = 3
  ) +
  scale_x_continuous(
    breaks = c(1, 2, 3),
    labels = c("4h_vs_0h", "24h_vs_0h", "24h_vs_4h"),
    limits = c(0.45, 3.2)
  ) +
  scale_color_manual(
    values = c("FALSE" = "grey80", "TRUE" = "black"),
    labels = c("Niet aanwezig", "Aanwezig"),
    name = ""
  ) +
  labs(
    title = "Overlap van relevante Metascape-pathways",
    x = "Vergelijking",
    y = "Pathway",
    fill = "Categorie"
  ) +
  theme_bw() +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1),
    axis.text.y = element_text(size = 8),
    plot.title = element_text(face = "bold"),
    legend.position = "right",
    panel.grid.major.y = element_line(color = "grey90"),
    panel.grid.minor = element_blank()
  )
```

#### 04-06-2026

Toen ik mijn overlap grafiek en de heatmap wou verwerken in het artikel, kwam ik erachter dat ik de tijdspunten verkeerd heb geïnterpreteerd. A blijkt dus 24h te zijn in plaats van 0h en C is 0h in plaats van 24h. Wat ik nu opnieuw moet doen is de DEG-bestanden hernoemen, de inputbestanden voor Metascape opnieuw maken, Metascape opnieuw runnen en de grafieken opnieuw maken. 

```{r DEGs opnieuw inlezen}

deg_ab <- read.csv("/students/2025-2026/Thema08/lipolyse/unpacked/transcriptomics/significant_DEG_AB_rna.csv")
deg_ac <- read.csv("/students/2025-2026/Thema08/lipolyse/unpacked/transcriptomics/significant_DEG_AC_rna.csv")
deg_bc <- read.csv("/students/2025-2026/Thema08/lipolyse/unpacked/transcriptomics/significant_DEG_BC_rna.csv")


correct_deg_24h_vs_4h <- deg_ab
correct_deg_24h_vs_0h <- deg_ac
correct_deg_4h_vs_0h  <- deg_bc

```

```{r}
make_metascape_file <- function(dap_genes, deg_genes, output_file) {
  
  dap_genes <- unique(na.omit(dap_genes))
  deg_genes <- unique(na.omit(deg_genes))
  
  max_length <- max(length(dap_genes), length(deg_genes))
  
  dap_genes <- c(dap_genes, rep(NA, max_length - length(dap_genes)))
  deg_genes <- c(deg_genes, rep(NA, max_length - length(deg_genes)))
  
  metascape_input <- data.frame(
    DAP = dap_genes,
    DEG = deg_genes
  )
  
  write.csv(
    metascape_input,
    output_file,
    row.names = FALSE,
    quote = FALSE,
    na = ""
  )
}

make_metascape_file(
  dap_genes = dap_4h_vs_0h_sig$ensembl_gene_id,
  deg_genes = correct_deg_4h_vs_0h$Gene,
  output_file = "/students/2025-2026/Thema08/lipolyse/unpacked/Metascape_input/Correct_Metascape_4h_vs_0h.csv"
)

make_metascape_file(
  dap_genes = dap_24h_vs_0h_sig$ensembl_gene_id,
  deg_genes = correct_deg_24h_vs_0h$Gene,
  output_file = "/students/2025-2026/Thema08/lipolyse/unpacked/Metascape_input/Correct_Metascape_24h_vs_0h.csv"
)

make_metascape_file(
  dap_genes = dap_24h_vs_4h_sig$ensembl_gene_id,
  deg_genes = correct_deg_24h_vs_4h$Gene,
  output_file = "/students/2025-2026/Thema08/lipolyse/unpacked/Metascape_input/Correct_Metascape_24h_vs_4h.csv"
)
```

```{r}
metascape_files_controle <- data.frame(
  Bestand = c(
    "Metascape_4h_vs_0h.csv",
    "Metascape_24h_vs_0h.csv",
    "Metascape_24h_vs_4h.csv"
  ),
  Bestaat = c(
    file.exists("/students/2025-2026/Thema08/lipolyse/unpacked/Metascape_input/Correct_Metascape_4h_vs_0h.csv"),
    file.exists("/students/2025-2026/Thema08/lipolyse/unpacked/Metascape_input/Correct_Metascape_24h_vs_0h.csv"),
    file.exists("/students/2025-2026/Thema08/lipolyse/unpacked/Metascape_input/Correct_Metascape_24h_vs_4h.csv")
  ),
  Aantal_DAPs = c(
    length(unique(na.omit(dap_4h_vs_0h_sig$ensembl_gene_id))),
    length(unique(na.omit(dap_24h_vs_0h_sig$ensembl_gene_id))),
    length(unique(na.omit(dap_24h_vs_4h_sig$ensembl_gene_id)))
  ),
  Aantal_DEGs = c(
    length(unique(na.omit(correct_deg_4h_vs_0h$Gene))),
    length(unique(na.omit(correct_deg_24h_vs_0h$Gene))),
    length(unique(na.omit(correct_deg_24h_vs_4h$Gene)))
  )
)

knitr::kable(metascape_files_controle, row.names = FALSE)
```
Ik heb Metascape opnieuw uitgevoerd, en nu ga ik de resultaten opnieuw inlezen, filteren en aangepaste plots maken.

```{r correcte metascape resultaten inlezen}

metascape_4h_vs_0h_correct <- read_excel(
  "/students/2025-2026/Thema08/lipolyse/unpacked/Metascape_output/metascape_result_4hvs_0h_correct.xlsx",
  sheet = "Enrichment"
)

metascape_24h_vs_0h_correct <- read_excel(
  "/students/2025-2026/Thema08/lipolyse/unpacked/Metascape_output/metascape_result_24hvs0h_correct.xlsx",
  sheet = "Enrichment"
)

metascape_24h_vs_4h_correct <- read_excel(
  "/students/2025-2026/Thema08/lipolyse/unpacked/Metascape_output/metascape_result_24hvs4h_correct.xlsx",
  sheet = "Enrichment"
)


```

Controleren of de nieuwe bestanden goed zijn gemaakt en ingelezen
```{r correcte metascape check}
metascape_check <- data.frame(
  Vergelijking = c("4h_vs_0h", "24h_vs_0h", "24h_vs_4h"),
  Aantal_rijen = c(
    nrow(metascape_4h_vs_0h_correct),
    nrow(metascape_24h_vs_0h_correct),
    nrow(metascape_24h_vs_4h_correct)
  ),
  Aantal_kolommen = c(
    ncol(metascape_4h_vs_0h_correct),
    ncol(metascape_24h_vs_0h_correct),
    ncol(metascape_24h_vs_4h_correct)
  ),
  Kolomnamen = c(
    paste(colnames(metascape_4h_vs_0h_correct), collapse = ", "),
    paste(colnames(metascape_24h_vs_0h_correct), collapse = ", "),
    paste(colnames(metascape_24h_vs_4h_correct), collapse = ", ")
  )
)

knitr::kable(metascape_check, row.names = FALSE)
```
Ze komen relatief overeen met de eerdere check voor de metascape resultaten, alleen heeft 4h_vs_0h nu 1 rij meer. 

Ik ga nu weer filteren op 'summary' en dan de controle uitvoeren of het goed is gefilterd.

```{r}
metascape_summary_4h_vs_0h_correct <- metascape_4h_vs_0h_correct %>%
  filter(grepl("Summary", GroupID))

metascape_summary_24h_vs_0h_correct <- metascape_24h_vs_0h_correct %>%
  filter(grepl("Summary", GroupID))

metascape_summary_24h_vs_4h_correct <- metascape_24h_vs_4h_correct %>%
  filter(grepl("Summary", GroupID))
```


```{r}
summary_controle_correct <- data.frame(
  Vergelijking = c("4h_vs_0h", "24h_vs_0h", "24h_vs_4h"),
  Aantal_termen_totaal = c(
    nrow(metascape_4h_vs_0h_correct),
    nrow(metascape_24h_vs_0h_correct),
    nrow(metascape_24h_vs_4h_correct)
  ),
  Aantal_summary_termen = c(
    nrow(metascape_summary_4h_vs_0h_correct),
    nrow(metascape_summary_24h_vs_0h_correct),
    nrow(metascape_summary_24h_vs_4h_correct)
  )
)

knitr::kable(summary_controle_correct, row.names = FALSE)
```
Het is opvallend dat ik nu ook weer 20 summary termen heb net als de vorige keer wanneer ik de metascape resultaten gefilterd heb op summary. 

Ik ga nu gelijk filteren op de uitgebreide relevante termen lijst. 

```{r}
metascape_summary_4h_vs_0h_uitgebreid_correct <- metascape_summary_4h_vs_0h_correct %>%
  mutate(
    relevant_thermogenese_metabolisme_uitgebreid = grepl(
      paste(uitgebreide_relevante_termen, collapse = "|"),
      Description,
      ignore.case = TRUE
    ),
    housekeeping = grepl(
      paste(housekeeping_termen, collapse = "|"),
      Description,
      ignore.case = TRUE
    )
  )

metascape_summary_24h_vs_0h_uitgebreid_correct <- metascape_summary_24h_vs_0h_correct %>%
  mutate(
    relevant_thermogenese_metabolisme_uitgebreid = grepl(
      paste(uitgebreide_relevante_termen, collapse = "|"),
      Description,
      ignore.case = TRUE
    ),
    housekeeping = grepl(
      paste(housekeeping_termen, collapse = "|"),
      Description,
      ignore.case = TRUE
    )
  )

metascape_summary_24h_vs_4h_uitgebreid_correct <- metascape_summary_24h_vs_4h_correct %>%
  mutate(
    relevant_thermogenese_metabolisme_uitgebreid = grepl(
      paste(uitgebreide_relevante_termen, collapse = "|"),
      Description,
      ignore.case = TRUE
    ),
    housekeeping = grepl(
      paste(housekeeping_termen, collapse = "|"),
      Description,
      ignore.case = TRUE
    )
  )
```

Deze lijst ga ik weer controleren of het goed uitgevoerd is. 

```{r}
filter_controle_uitgebreid_correct <- data.frame(
  Vergelijking = c("4h_vs_0h", "24h_vs_0h", "24h_vs_4h"),
  Summary_termen = c(
    nrow(metascape_summary_4h_vs_0h_uitgebreid_correct),
    nrow(metascape_summary_24h_vs_0h_uitgebreid_correct),
    nrow(metascape_summary_24h_vs_4h_uitgebreid_correct)
  ),
  Uitgebreide_relevante_termen = c(
    sum(metascape_summary_4h_vs_0h_uitgebreid_correct$relevant_thermogenese_metabolisme_uitgebreid),
    sum(metascape_summary_24h_vs_0h_uitgebreid_correct$relevant_thermogenese_metabolisme_uitgebreid),
    sum(metascape_summary_24h_vs_4h_uitgebreid_correct$relevant_thermogenese_metabolisme_uitgebreid)
  ),
  Housekeeping_termen = c(
    sum(metascape_summary_4h_vs_0h_uitgebreid_correct$housekeeping),
    sum(metascape_summary_24h_vs_0h_uitgebreid_correct$housekeeping),
    sum(metascape_summary_24h_vs_4h_uitgebreid_correct$housekeeping)
  )
)

knitr::kable(filter_controle_uitgebreid_correct, row.names = FALSE)
```
De relevante termen zijn in dit geval ook hetzelfde als de eerste keer dat ik de resultaten verwerkte. Ik ga kijken of hier ook nieuwe pathways uit zijn gekomen of dat ze hetzelfde zijn. 

```{r}
relevante_pathways_tabel_correct <- bind_rows(
  metascape_summary_4h_vs_0h_uitgebreid_correct %>%
    filter(relevant_thermogenese_metabolisme_uitgebreid == TRUE) %>%
    mutate(Vergelijking = "4h_vs_0h"),
  
  metascape_summary_24h_vs_0h_uitgebreid_correct %>%
    filter(relevant_thermogenese_metabolisme_uitgebreid == TRUE) %>%
    mutate(Vergelijking = "24h_vs_0h"),
  
  metascape_summary_24h_vs_4h_uitgebreid_correct %>%
    filter(relevant_thermogenese_metabolisme_uitgebreid == TRUE) %>%
    mutate(Vergelijking = "24h_vs_4h")
) %>%
  select(
    Vergelijking,
    Category,
    Description,
    LogP,
    `Log(q-value)`
  ) %>%
  arrange(Vergelijking, `Log(q-value)`)

knitr::kable(relevante_pathways_tabel_correct, row.names = FALSE, digits = 3)
```
Ik kan niet direct een grote verandering zien, maar misschien wordt er wel wat zichtbaar in de heatmap en/of de overlap grafiek. 

```{r}
heatmap_data_correct <- relevante_pathways_tabel_correct %>%
  mutate(
    enrichment_score = -`Log(q-value)`,
    Description_clean = gsub(" - Mus musculus \\(house mouse\\)", "", Description),
    
    pathway_groep = case_when(
      grepl(
        "mitochond|TCA|citrate cycle|electron transport|oxidative phosphorylation|protein localization to mitochondrion",
        Description_clean,
        ignore.case = TRUE
      ) ~ "Mitochondrial / energy",
      
      grepl(
        "carbon metabolism|pyruvate|precursor metabolites|NADP",
        Description_clean,
        ignore.case = TRUE
      ) ~ "Energy metabolism",
      
      grepl(
        "amino acid|valine|leucine|isoleucine|tRNA aminoacylation",
        Description_clean,
        ignore.case = TRUE
      ) ~ "Amino acid metabolism",
      
      grepl(
        "acyl-CoA|dicarboxylic acid|monocarboxylic acid|fatty acid|lipid",
        Description_clean,
        ignore.case = TRUE
      ) ~ "Fatty acid / organic acid metabolism",
      
      grepl(
        "small molecule",
        Description_clean,
        ignore.case = TRUE
      ) ~ "Small molecule metabolism",
      
      TRUE ~ "Other"
    )
  ) %>%
  select(Description_clean, pathway_groep, Vergelijking, enrichment_score) %>%
  pivot_wider(
    names_from = Vergelijking,
    values_from = enrichment_score,
    values_fill = 0
  ) %>%
  arrange(
    factor(
      pathway_groep,
      levels = c(
        "Fatty acid / organic acid metabolism",
        "Mitochondrial / energy",
        "Energy metabolism",
        "Amino acid metabolism",
        "Small molecule metabolism",
        "Other"
      )
    ),
    Description_clean
  )

heatmap_matrix_correct <- as.data.frame(heatmap_data_correct)
rownames(heatmap_matrix_correct) <- heatmap_matrix_correct$Description_clean

row_annotation_correct <- data.frame(
  Category = heatmap_matrix_correct$pathway_groep
)
rownames(row_annotation_correct) <- rownames(heatmap_matrix_correct)

heatmap_matrix_correct <- heatmap_matrix_correct %>%
  select(-Description_clean, -pathway_groep)

pheatmap(
  as.matrix(heatmap_matrix_correct),
  cluster_rows = FALSE,
  cluster_cols = FALSE,
  annotation_row = row_annotation_correct,
  main = "Pathways per comparison",
  angle_col = 90,
  fontsize_row = 7,
  fontsize_col = 9,
  fontsize = 9,
  treeheight_row = 0,
  treeheight_col = 0,
  legend = TRUE,
  annotation_legend = TRUE
)
```
In de heatmap is geen duidelijk verschil.

```{r}
pathway_overlap_plot_correct <- relevante_pathways_tabel_correct %>%
  mutate(
    Pathway = gsub(" - Mus musculus \\(house mouse\\)", "", Description),
    aanwezig = TRUE,
    
    pathway_groep = case_when(
      grepl(
        "mitochond|TCA|citrate cycle|electron transport|oxidative phosphorylation|protein localization to mitochondrion",
        Pathway,
        ignore.case = TRUE
      ) ~ "Mitochondrial / energy",
      
      grepl(
        "carbon metabolism|pyruvate|precursor metabolites|NADP",
        Pathway,
        ignore.case = TRUE
      ) ~ "Energy metabolism",
      
      grepl(
        "amino acid|valine|leucine|isoleucine|tRNA aminoacylation",
        Pathway,
        ignore.case = TRUE
      ) ~ "Amino acid metabolism",
      
      grepl(
        "acyl-CoA|dicarboxylic acid|monocarboxylic acid|fatty acid|lipid",
        Pathway,
        ignore.case = TRUE
      ) ~ "Fatty acid / organic acid metabolism",
      
      grepl(
        "small molecule",
        Pathway,
        ignore.case = TRUE
      ) ~ "Small molecule metabolism",
      
      TRUE ~ "Overig"
    )
  ) %>%
  select(Pathway, pathway_groep, Vergelijking, aanwezig) %>%
  distinct()

pathway_info <- pathway_overlap_plot_correct %>%
  distinct(Pathway, pathway_groep)

pathway_overlap_plot_correct <- pathway_overlap_plot_correct %>%
  select(Pathway, Vergelijking, aanwezig) %>%
  complete(
    Pathway,
    Vergelijking = c("4h_vs_0h", "24h_vs_0h", "24h_vs_4h"),
    fill = list(aanwezig = FALSE)
  ) %>%
  left_join(pathway_info, by = "Pathway") %>%
  group_by(Pathway) %>%
  mutate(
    aantal_vergelijkingen = sum(aanwezig)
  ) %>%
  ungroup() %>%
  arrange(
    factor(
      pathway_groep,
      levels = c(
          "Fatty acid / organic acid metabolism",
          "Mitochondrial / energy",
          "Energy metabolism",
          "Amino acid metabolism",
          "Small molecule metabolism",
          "Other"
      )
    ),
    desc(aantal_vergelijkingen),
    Pathway
  ) %>%
  mutate(
    Pathway = factor(Pathway, levels = rev(unique(Pathway))),
    Vergelijking = factor(
      Vergelijking,
      levels = c("4h_vs_0h", "24h_vs_0h", "24h_vs_4h")
    ),
    x_num = as.numeric(Vergelijking)
  )

pathway_lijnen <- pathway_overlap_plot_correct %>%
  filter(aanwezig == TRUE) %>%
  group_by(Pathway) %>%
  summarise(
    x_start = min(x_num),
    x_end = max(x_num),
    .groups = "drop"
  ) %>%
  filter(x_start != x_end)

categorie_balk_correct <- pathway_overlap_plot_correct %>%
  distinct(Pathway, pathway_groep)

ggplot(pathway_overlap_plot_correct, aes(x = x_num, y = Pathway)) +
  geom_tile(
    data = categorie_balk_correct,
    aes(x = 0.55, y = Pathway, fill = pathway_groep),
    inherit.aes = FALSE,
    width = 0.12,
    height = 0.8
  ) +
  geom_segment(
    data = pathway_lijnen,
    aes(
      x = x_start,
      xend = x_end,
      y = Pathway,
      yend = Pathway
    ),
    inherit.aes = FALSE,
    linewidth = 0.6
  ) +
  geom_point(
    aes(color = aanwezig),
    size = 3
  ) +
  scale_x_continuous(
    breaks = c(1, 2, 3),
    labels = c("4h_vs_0h", "24h_vs_0h", "24h_vs_4h"),
    limits = c(0.45, 3.2)
  ) +
  scale_color_manual(
    values = c("FALSE" = "grey80", "TRUE" = "black"),
    labels = c("Absent", "Present"),
    name = ""
  ) +
  labs(
    title = "Shared pathways between comparisons",
    x = "Comparison",
    y = "Pathway",
    fill = "Category"
  ) +
  theme_bw() +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1),
    axis.text.y = element_text(size = 7),
    plot.title = element_text(face = "bold"),
    legend.position = "right",
    panel.grid.major.y = element_line(color = "grey90"),
    panel.grid.minor = element_blank()
  )
```

Omdat hier ook geen verschil in zit ga ik de pathways 1 op 1 vergelijken of er op deze manier verschillen aanwezig zijn. 
```{r}
cat("Aantal oude relevante pathways:", nrow(relevante_pathways_tabel), "\n")
cat("Aantal correcte relevante pathways:", nrow(relevante_pathways_tabel_correct), "\n\n")

cat("Pathways in correct maar niet in oud:\n")
print(setdiff(
  paste(relevante_pathways_tabel_correct$Vergelijking,
        relevante_pathways_tabel_correct$Description),
  paste(relevante_pathways_tabel$Vergelijking,
        relevante_pathways_tabel$Description)
))

cat("\nPathways in oud maar niet in correct:\n")
print(setdiff(
  paste(relevante_pathways_tabel$Vergelijking,
        relevante_pathways_tabel$Description),
  paste(relevante_pathways_tabel_correct$Vergelijking,
        relevante_pathways_tabel_correct$Description)
))
```

Er zijn dus geen nieuwe pathways gevonden, maar wat ik nog wel kan controleren of de LogP en Log(q-value) zijn veranderd. 

```{r}
score_vergelijking <- relevante_pathways_tabel %>%
  rename(
    LogP_oud = LogP,
    LogQ_oud = `Log(q-value)`
  ) %>%
  inner_join(
    relevante_pathways_tabel_correct %>%
      rename(
        LogP_correct = LogP,
        LogQ_correct = `Log(q-value)`
      ),
    by = c("Vergelijking", "Category", "Description")
  ) %>%
  mutate(
    verschil_LogP = LogP_correct - LogP_oud,
    verschil_LogQ = LogQ_correct - LogQ_oud
  )

summary(score_vergelijking$verschil_LogP)
summary(score_vergelijking$verschil_LogQ)

knitr::kable(
  score_vergelijking %>%
    filter(verschil_LogP != 0 | verschil_LogQ != 0),
  row.names = FALSE,
  digits = 3
)
```
Hier is te zien dat er alleen verschil is gevonden in de vergelijking 4h vs 0h. Er is te zien dat de LogP en Log(q-value) licht is veranderd in vergelijking tot de LogP en Log(q-value) die de pathways hebben. Wat dit uiteindelijk betekent is dat ik wel de gecorrigeerde plots ga gebruiken in het artikel, maar in zijn geheel is er weinig veranderd. 

### Referenties

Metascape is gebruikt voor de enrichment-analyse: [Metascape](https://metascape.org/gp/index.html#/main/step1)
Beschrijving van de metascape tool: [Metascape artikel](https://pubmed.ncbi.nlm.nih.gov/30944313/)
Lipolyse pathways: [Lipolyse pathways](https://www.frontiersin.org/journals/endocrinology/articles/10.3389/fendo.2023.1106544/full) 
